#pragma once

namespace map{
    namespace Config{
        inline decltype(sizeof(0)) height;
        inline decltype(sizeof(0)) width;
        // inline decltype(sizeof(0)) height =
        //     #include "height.txt"
        // ;

        // inline decltype(sizeof(0)) width =
        //     #include "width.txt"
        // ;
    };

}
